swagger: "2.0"
info:
  title: "CodeBrincando API"
  description: >
    API do CodeBrincando, usada pelo frontend para cadastro de usuários,
    controle de desafios e integração com a RoboTeca (chatbot de IA via Groq API).
  version: "1.0.0"
host: "localhost:5001"
basePath: "/"
schemes:
  - "http"
consumes:
  - "application/json"
produces:
  - "application/json"
paths: {}
